{
  "name": "recycling-bin-locator",
  "private": true,
  "version": "1.0.0",
  "description": "Static site for finding recycling bins in Singapore",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.4"
  }
}
